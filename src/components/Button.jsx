
const Button = ({children}) => {
  return (
    <button>{children}</button>
  )
}

export default Button



// const Button = (prop) => {
//     return (
//       <button>{prop.children}</button>
//     )
//   }
  
//   export default Button